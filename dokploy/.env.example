# =============================================================================
# QA Guardian - Dokploy Environment Variables (Quick Reference)
# =============================================================================
# For the COMPLETE configuration file with detailed documentation, see:
#   .env.production.example
#
# This file is a minimal quick-start template.
# Configure these in Dokploy dashboard under Environment Variables.
# Domain: qa.pixelcraftedmedia.com
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Application Mode
# -----------------------------------------------------------------------------
NODE_ENV=production

# -----------------------------------------------------------------------------
# REQUIRED - Database (Generate with: openssl rand -base64 32)
# -----------------------------------------------------------------------------
POSTGRES_PASSWORD=<generate-with-openssl-rand-base64-32>

# -----------------------------------------------------------------------------
# REQUIRED - Redis (Generate with: openssl rand -base64 32)
# -----------------------------------------------------------------------------
REDIS_PASSWORD=<generate-with-openssl-rand-base64-32>

# -----------------------------------------------------------------------------
# REQUIRED - MinIO Storage
# -----------------------------------------------------------------------------
MINIO_ROOT_USER=qa-guardian-admin
MINIO_ROOT_PASSWORD=<generate-with-openssl-rand-base64-32>
STORAGE_BUCKET=qa-guardian-artifacts

# -----------------------------------------------------------------------------
# REQUIRED - Authentication Secrets (Generate with: openssl rand -base64 64)
# -----------------------------------------------------------------------------
JWT_SECRET=<generate-with-openssl-rand-base64-64>
JWT_REFRESH_SECRET=<generate-with-openssl-rand-base64-64>
SESSION_SECRET=<generate-with-openssl-rand-base64-64>

# -----------------------------------------------------------------------------
# REQUIRED - Frontend URLs
# -----------------------------------------------------------------------------
FRONTEND_URL=https://qa.pixelcraftedmedia.com
VITE_API_URL=https://qa.pixelcraftedmedia.com
VITE_API_BASE_URL=https://qa.pixelcraftedmedia.com
VITE_SOCKET_URL=https://qa.pixelcraftedmedia.com
VITE_MCP_URL=https://qa.pixelcraftedmedia.com/mcp

# -----------------------------------------------------------------------------
# OPTIONAL - AI Providers (enables MCP Chat, test generation, AI analysis)
# -----------------------------------------------------------------------------
AI_PROVIDER_PRIMARY=kie
AI_PROVIDER_FALLBACK=anthropic
KIE_API_KEY=
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# OPTIONAL - Google OAuth
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# OPTIONAL - GitHub Integration
# -----------------------------------------------------------------------------
GITHUB_APP_ID=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Version (optional)
# -----------------------------------------------------------------------------
VERSION=latest
