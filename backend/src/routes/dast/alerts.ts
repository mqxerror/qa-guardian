// DAST Alert Generation
//
// Previously contained generateSimulatedAlerts() which produced hardcoded
// vulnerability templates. All alert data now comes from the real ZAP API
// response in scanner.ts. This module is intentionally empty; the export
// is retained in index.ts for backward compatibility but can be removed
// once all consumers are verified.
